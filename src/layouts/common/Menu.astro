---
import BarMenu from "./BarMenu.astro";
import FullMenu from "./FullMenu.astro";

export interface Props {
    currentPage: string
}

const { currentPage } = Astro.props
---

<header>
    <BarMenu currentPage={currentPage} />
    <FullMenu />
</header>