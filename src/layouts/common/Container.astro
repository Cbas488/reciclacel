<section class="max-w-full xl:max-w-6xl 2xl:max-w-8xl mx-auto px-5 py-44 flex flex-col">
    <span class="inline-block uppercase font-semibold font-archivo pl-5 mb-2 relative">sobre nosotros</span>
    <slot/>
</section>

<style>
    span::before {
        content: "";
        position: absolute;
        left: 0;
        top: 50%;
        -webkit-transform: translateY(-50%);
        -ms-transform: translateY(-50%);
        transform: translateY(-50%);
        height: 2px;
        width: 1000%;
        left: -1000%;
        background: #4BB3FD;
    }
</style>