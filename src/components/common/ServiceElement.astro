---
export interface Props{
    number: string
    title: string
    text: string
    nombre: string
}

const { number, title, text, nombre } = Astro.props
---

<div class="flex flex-row border border-gray-300">
    <div class="w-5 border bg-gray-300"></div>
    <div class="flex flex-col p-8 gap-5 justify-between transition-colors duration-500 border border-gray-300 hover:border-reciclacel_green">
        <div class="w-full flex flex-col gap-5 mb-5">
            <span class="text-5xl font-archivo text-gray-200">{ number }</span>
            <h3 class="text-3xl tracking-tight leading-normal font-medium font-roboto first-letter:uppercase">{ title }</h3>
            <p class="text-xl font-roboto text-[#202C38]">{ text }</p>
        </div>
        <a class="text-lg uppercase font-medium font-roboto tracking-widest flex flex-row items-center gap-1 text-reciclacel_green" href=`/service/${nombre}`>
            <span>leer más</span>
            <i class="bi bi-arrow-right-short text-3xl"></i>
        </a>
    </div>
</div>